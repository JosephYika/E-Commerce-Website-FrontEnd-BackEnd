{
    /*Connecting to the database . Local host is what we are connecting to and the other is just an alternitive. At this point we can 
    install Nuget Packages . Go to Tools --> Nuget Package Manager --> Nuget Packges for Solution --> in search find two things: 
    1. Microsoft.EntityFrameWorkCore -- and install the version that matches the current project version e.g. v5.0 then v5.0 
    2. Microsoft.EntityFrameWorkCore.SqlServer -- same thing as for the first one
    */
    "ConnectionStrings": {
        "DefaultConnection": "Server=localhost;Database=E-Commerce-Website;Trusted_Connection=True;MultipleActiveResultSets=True;"
        // "DefaultConnection": "Server=DESKTOP-B6MLHE6; Database=E-Commerce-Website; Trusted_Connection=True; MultipleActiveResultsSets=True"
    },

    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft": "Warning",
            "Microsoft.Hosting.Lifetime": "Information"
        }
    },
    "AllowedHosts": "*"
}
